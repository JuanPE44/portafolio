---
type Props = {
  text: string;
  id: string;
  margin?: boolean;
};

const { text, id, margin = true } = Astro.props;
---

<h2
  class=`font-bold text-2xl dark:text-stone-100 min-w-fit ${margin ? "mb-7 mt-40" : ""}`
  id={id}
>
  {text}<span class="text-rainbow text-4xl">.</span>
</h2>
