---
interface Props {
  name: string;
  className?: string;
}
const { name, className } = Astro.props;
---

<span
  class={`${className} text-xs font-medium me-2 px-2.5 py-1 rounded-2xl`}
>
  {name}
</span>
