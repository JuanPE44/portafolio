<section class="relative pb-10 card-skill">
  <div
    class="text-center px-6 py-12 md:p-16 rounded-2xl border border-color-border backdrop-blur-sm services-cta-card"
  >
    <!-- Content -->
    <div class="relative z-10">
      <!-- Icon/Image -->
      <div class="mb-6 flex justify-center">
        <div class="relative">
          <div
            class="w-20 h-20 md:w-24 md:h-24 services-cta-icon rounded-2xl flex items-center justify-center shadow-lg"
          >
            <svg
              width="40"
              height="40"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="text-white/90"
            >
              <path d="M12 2L2 7l10 5 10-5-10-5z"></path>
              <path d="m2 17 10 5 10-5"></path>
              <path d="m2 12 10 5 10-5"></path>
            </svg>
          </div>
          <!-- Glow effect -->
          <div
            class="absolute inset-0 services-cta-icon-glow rounded-2xl blur-xl opacity-30 -z-10"
          >
          </div>
        </div>
      </div>

      <h3
        class="text-2xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-white/90 mb-3 md:mb-5 leading-tight"
      >
        ¿Tenés un negocio y querés
        <span class="text-secondary"> llevarlo al siguiente nivel? </span>
      </h3>

      <p
        class="text-base md:text-xl text-foreground-secondary mb-10 max-w-2xl mx-auto leading-relaxed"
      >
        Hablemos sin compromiso. Contame qué necesitás y te ayudo a hacerlo
        <span class="text-white/90 font-medium">realidad</span>.
      </p>

      <!-- CTA Button -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          href="#contact"
          class="group inline-flex items-center gap-3 bg-primary text-white/90 py-4 px-8 md:py-5 md:px-10 rounded-2xl font-semibold transition-all duration-300 ease-out hover:scale-105 hover:shadow-2xl transform"
        >
          Quiero mi sitio web
          <svg
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="transition-transform duration-300 group-hover:translate-x-1"
          >
            <path d="m9 18 6-6-6-6"></path>
          </svg>
        </a>

        <!-- Secondary CTA -->
        <a
          href="/#works"
          class="inline-flex items-center gap-2 text-white/90 text-lg font-medium hover:text-white/90 transition-colors duration-200 border-b border-color-border hover:border-white/90 pb-1"
        >
          Ver mi trabajo
          <svg
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M7 17L17 7"></path>
            <path d="M7 7h10v10"></path>
          </svg>
        </a>
      </div>

      <!-- Trust indicators -->
      <div class="hidden mt-12 pt-8 border-t border-color-border">
        <div
          class="flex-col md:flex-row flex justify-center items-center gap-3 md:gap-6 opacity-60"
        >
          <!-- Placeholder for client logos or stats -->
          <div class="flex items-center gap-2">
            <div class="w-2 h-2 bg-secondary rounded-full"></div>
            <span class="text-sm text-foreground-secondary">100% Satisfacción</span>
          </div>
          <div class="flex items-center gap-2">
            <div class="w-2 h-2 bg-primary rounded-full"></div>
            <span class="text-sm text-foreground-secondary">Entrega rápida</span>
          </div>
          <div class="flex items-center gap-2">
            <div class="w-2 h-2 bg-gray rounded-full"></div>
            <span class="text-sm text-foreground-secondary">Soporte 24/7</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Solo los gradientes complejos que no se pueden hacer con Tailwind */
  .services-cta-card {
    background: linear-gradient(
      135deg,
      var(--color-gray-bg),
      rgba(58, 65, 53, 0.8)
    );
  }

  .services-cta-overlay {
    background: linear-gradient(
      to right,
      rgba(225, 38, 235, 0.05),
      rgba(119, 235, 38, 0.05),
      rgba(255, 215, 0, 0.05)
    );
  }

  .services-cta-icon {
    background: linear-gradient(
      135deg,
      var(--color-primary),
      var(--color-secondary)
    );
  }

  .services-cta-icon-glow {
    background: linear-gradient(
      135deg,
      var(--color-primary),
      var(--color-secondary)
    );
  }

  /* Animación personalizada */
  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  .float-animation {
    animation: float 6s ease-in-out infinite;
  }
</style>
