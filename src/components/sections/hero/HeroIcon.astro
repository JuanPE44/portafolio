---
interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<div
  class=`${className}  cursor-pointer p-5 lg:p-7 bg-[#d8d8d896] filter backdrop-blur-lg border border-[#ffffff5e] absolute  rounded-full`
>
  <slot />
</div>
