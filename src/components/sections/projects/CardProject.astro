---
import IconGithub from "src/icons/IconGithub.astro";
interface Props {
  title: string;
  urlDemo: string;
  urlGithub: string;
}
const { title, urlDemo, urlGithub } = Astro.props;
---

<div class="flex w-full flex-col gap-4">
  <div
    class="bg-gray-bg w-full flex items-center justify-between px-4 py-3 rounded-2xl border-[#fff1] border-[1px]"
  >
    <h3 class="text-secondary text-xl md:text-3xl font-bold text-left">
      {title}
    </h3>
    <div class="flex gap-2 items-center">
      <a
        target="_blank"
        class="px-4 py-2 rounded-2xl bg-primary text-white font-bold hover:scale-105 transition-[0.3s]"
        href={urlDemo}>Demo</a
      >
      <a
        target="_blank"
        href={urlGithub}
        class="flex gap-2 px-4 py-2 rounded-2xl bg-secondary text-bg font-bold hover:scale-105 transition-[0.3s]"
      >
        <IconGithub className="fill-bg" width="20" height="20" />
        <span>Codigo</span>
      </a>
    </div>
  </div>
  <div
    class="flex justify-center items-center bg-gray-bg rounded-2xl px-4 py-10 relative gap-4 border-[#fff1] border-[1px]"
  >
    <slot />
  </div>
</div>
